
#trigger:
#- main

pr:
  branches:
    include:
    - reviewapps 

pool:
  vmImage: 'ubuntu-latest'

stages:
- template: pipeline-templates/azure-build-pipelines.yml
- template: pipeline-templates/azure-release.yml