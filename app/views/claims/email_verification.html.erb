<% content_for(:page_title, page_title(t("one_time_password"), policy: current_policy_routing_name, show_error: current_claim.errors.any?)) %>

<%= render("one_time_password", locals: {current_claim: current_claim, current_policy_routing_name: current_policy_routing_name}) %>
