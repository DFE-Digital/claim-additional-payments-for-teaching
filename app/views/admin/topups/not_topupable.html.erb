<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <span class="govuk-caption-xl"><%= @claim.policy.short_name %> (<%= @claim.academic_year %>)</span>
    <h1 class="govuk-heading-xl">
      Top up claim <%= @claim.reference %>
    </h1>

    <div class="govuk-body-l govuk-flash__alert">
      This claim cannot be topped up.
    </div>

    <p><%= link_to "Return to claim page", admin_claim_path(@claim), class: "govuk-link" %></p>
  </div>
</div>
