<% content_for(:page_title, page_title(@form.t("#{@form.ineligibility_reason}.heading"), journey: current_journey_routing_name, show_error: @form.errors.any?)) %>

<%= render "ineligible_#{@form.ineligibility_reason}" %>

<p class="govuk-body">
  <%= govuk_link_to "What did you think of this service?", feedback_path(journey: current_journey_routing_name, slug: "details") %>
</p>
