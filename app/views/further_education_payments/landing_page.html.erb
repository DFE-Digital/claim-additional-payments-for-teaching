<% content_for(:page_title, page_title(t("further_education_payments.landing_page"), journey: current_journey_routing_name)) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-xl"><%= t("further_education_payments.landing_page") %></h1>

    <p class="govuk-body">
      Use this service to claim a targeted retention incentive payment.
      The payment is only for <%= govuk_link_to "certain further education (FE) teachers", "https://www.gov.uk/guidance/targeted-retention-incentive-payments-for-fe-teachers" %> in the
      first 5 years of their teaching career.
    </p>

    <%= govuk_inset_text do %>
      This service uses GOV.UK One Login to prove your identity.
      You’ll be able to create a GOV.UK One Login if you do not already
      have one. You’ll need photo ID to prove your identity.
    <% end %>

    <%# TODO: There isn't any journey closed content, for now hide the button %>
    <% if @journey_open %>
      <%= govuk_start_button(text: "Start now", href: claim_path(current_journey_routing_name, "claim", answers: { service_access_code: params[:service_access_code] })) %>
    <% end %>
  </div>
</div>
